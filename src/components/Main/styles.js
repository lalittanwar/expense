import { makeStyles } from '@material-ui/core/styles';

export default makeStyles( () =>
( {
    header1: {
        fontFamily: 'system-ui',
        paddingTop: '12px',
        color: 'navy'
    }
} ) )